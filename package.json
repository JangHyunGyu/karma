{
  "name": "archerlab-karma",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node validate-i18n.js && node validate-quality.js && node validate-contracts.js && node validate-tarot.js && node --test tests/*.test.cjs"
  }
}
